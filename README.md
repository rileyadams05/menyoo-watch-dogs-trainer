# menyoo-watch-dogs-trainer

A Watch Dogs trainer built with Tauri and Rust, with native trainer components for Windows.

The project includes a Tauri launcher in `menyoo-launcher` and trainer code/assets used to build the Windows trainer.
